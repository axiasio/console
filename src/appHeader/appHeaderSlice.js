import {createSlice} from '@reduxjs/toolkit';

const initialState = {}

export const appHeaderSlice = createSlice({
    name: 'appHeader',
    initialState,
    reducers: {}
})

export const {} = appHeaderSlice.actions


export default appHeaderSlice.reducer