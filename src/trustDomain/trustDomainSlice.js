import {createSlice} from "@reduxjs/toolkit";

const initialState = {}

export const trustDomainSlice = createSlice({
    name: 'trustDomain',
    initialState,
    reducers: {},
    extraReducers: (builder) => {
    }
})

export const {} = trustDomainSlice.actions


export default trustDomainSlice.reducer